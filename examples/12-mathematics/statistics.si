# Population statistics and linearly interpolated percentiles.
measurements is [1, 2, 3, 4]

Say mean(measurements)
Say variance(measurements)
Say stddev(measurements)
Say median(measurements)
Say percentile(measurements, 25)
