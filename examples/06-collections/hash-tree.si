# Hashes and trees support fields, string indexing, and nesting
profile is hash:
    name is "Ada"
    role is "builder"
end

catalog is tree:
    featured is profile
end

Say profile.name
Say profile["role"]
Say catalog["featured"].name
