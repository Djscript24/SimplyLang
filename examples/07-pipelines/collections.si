# Filter, map, sum, and count in collection pipelines
numbers is list [10, 20, 30, 40]

total is pipeline:
    numbers
    filter item >= 20
    map item * 2
    sum
end

how_many is pipeline:
    numbers
    filter item >= 20
    count
end

Say total
Say how_many
